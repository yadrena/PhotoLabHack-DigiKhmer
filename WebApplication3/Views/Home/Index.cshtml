@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Приветствую тебя, дооброжелатель!</h1>
    <p>На этой страничке ты сможешь обгадить фотографию друга (ну или свою)</p>
</div>

<form method="post" enctype="multipart/form-data" asp-controller="UploadFiles" asp-action="Post">
    <div class="form-group">
        <div class="col-md-10">
            <p>Upload one or more files using this form:</p>
            <input type="file" name="files" multiple />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Upload" />
        </div>
    </div>
</form>
<div>

    @*@foreach (var provider in Model.ExternalLogins)
    {
        <p>
            <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>

        </p>
    }*@
</div>